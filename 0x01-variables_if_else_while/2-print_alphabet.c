#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * Main - > assign a random number to the variable n each time it is executed
 * and print the last digit of the number store
 * Return : Always 0
 */
int main(void)
{
int ch;
for (ch = 97; ch <= 122; ch++)
{
putcha(ch);
}
putcha("\n");

return(0);
}
